# 注意:torch / torchaudio 不在這裡裝 —— 由 install.ps1 用正確的 CUDA index-url 安裝
# (RTX 5060 / Blackwell 需要 cu128 版,一般 PyPI 的 torch 不支援 sm_120)

# --- ASR ---
faster-whisper>=1.1.0

# --- 人聲分離 ---
demucs>=4.0.1

# --- 強制對齊(完整歌詞模式) ---
# 用 torchaudio 內建的 forced_align + MMS_FA bundle(已隨 torchaudio wheel 預編譯)。
# 刻意「不用」ctc-forced-aligner —— 它有 C++ 擴充,Windows 需 Visual C++ Build Tools 才能
# 從原始碼建置(對一般使用者門檻太高)。torchaudio 原生路徑免編譯器、跨平台、效果相當。
# 見 pipeline/align.py。
#
# 選用:uroman(純 Python、免編譯)讓「中日韓」歌詞可羅馬化後逐字對齊(jpn/kor 後備)。
# 沒裝也能跑——CJK 強制對齊會退回下一層、或上層 pipeline 退回 biasing。
uroman>=1.3.1.1  # CJK 強制對齊後備(英文/拉丁語不需要)

# 選用(強烈建議,皆純 Python、免 C/C++ 編譯器):語言感知 CJK 羅馬化,大幅提升對齊精度。
#   - pypinyin:國語(zh)→ 拼音(無聲調即落在 MMS_FA a-z 字典,比 uroman 更貼近歌唱發音)
#   - pycantonese:粵語(yue)→ 粵拼 jyutping(詞context消歧;聲調數字事後剝除 → 純 a-z)
# 缺任一個都優雅降級(見 pipeline/romanize.py:zh→pinyin、yue→jyutping、其他→uroman→原樣)。
pypinyin>=0.51.0     # 選用:國語逐字拼音對齊
pycantonese>=3.4.0   # 選用:粵語逐字粵拼對齊

# --- 本地 API 服務 ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# --- 音訊 I/O ---
soundfile>=0.12
numpy>=1.24

# --- 影片文字 / 區域移除(inpaint;「文字移除 / Clean Text」模式)---
# OpenCV 提供免模型的 cv2.inpaint(Telea) 後備路徑(LaMa 不可用時自動退回)。
opencv-python>=4.8.0
#
# 注意:LaMa 引擎的 `simple-lama-inpainting` 刻意「不」列在這裡 —— 它必須用
#   pip install --no-deps simple-lama-inpainting
# 單獨安裝(見 install.ps1 / install.sh / lib.rs run_setup)。原因:它把
# numpy<2、pillow<10 釘得過保守,若隨依賴一起裝會把 cu128 技術棧的
# numpy 2.x / pillow 12 降版、連帶弄壞 torch。實測它在 numpy 2.x / pillow 12
# 上運作完全正常,故以 --no-deps 跳過那些過時的版本釘選。
# (av / PyAV 由 torch / faster-whisper 連帶帶入;影片解碼/編碼即靠它。)
